<div class="page-header">
	<h1><?= $this->translate('Customers') ?></h1>
	<a href="<?= $this->url('customers', ['action' => 'add'])?>" class="btn btn-default btn-lg" role="button"><?= $this->translate('New Customer') ?></a>
</div>

<table class="table table-striped">
	<thead>
    	<tr>
    		<th><?= $this->translate("Name") ?></th>
    		<th><?= $this->translate('Email')?></th>
    		<th><?= $this->translate('Phone')?></th>
    	</tr>
   	</thead>
   	<tbody>
<?php foreach ($paginator as $customer) : ?>
    	<tr>
    		<td><?= $this->escapeHtml($customer->name) ?></td>
    		<td><?= $this->escapeHtml($customer->email) ?></td>
    		<td><?= $this->escapeHtml($customer->phone) ?></td>
    	</tr>
<?php endforeach; ?>
	</tbody>
</table>

<?php
 // add at the end of the file after the table
 echo $this->paginationControl(
     // the paginator object
     $this->paginator,
     // the scrolling style
     'sliding',
     // the partial to use to render the control
     'partial/paginator.phtml',
     // the route to link to when a user clicks a control link
     array(
         'route' => 'customers'
     )
 );
 ?>